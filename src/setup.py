import setuptools 

setuptools.setup(
      version='1.0.0',
      description='',
      name="surrogate",
      packages=setuptools.find_packages('.'),
      package_dir={'': '.'},
      install_requires=[
      ]
     )
